It launches via allure serve tests\allure_results

1. Все тесты лежат в файле test_api.py

2. Файл samples не используется, сохранил шаблоны из документации для быстрого доступа и просмотра.

3. Не разобрался, что имелось в виду под body в документации.

4. При сортировке по статусу сервер возвращает список из десятков Pet. По отдельности Пет валидируется, но
провалидировать модель из списка Пет - провалилось. Не помог ни дефолтное присваивание None, ни Optional, ни Field.
Вижу проблему в том, что некоторые Пет имеют все поля, некоторые не все, из-за этого не смог правильно настроить
динамическую валидацию.
